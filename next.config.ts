// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
	images: {
		domains: ['shimatoworks.xsrv.jp', 'assets.st-note.com']
	}
};

export default nextConfig;
